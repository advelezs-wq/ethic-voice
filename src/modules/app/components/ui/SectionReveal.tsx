// Re-exported so dashboard code has one place to import the shared entrance-animation
// primitive from — the actual implementation lives in the landing module and is shared,
// not duplicated, to keep the two design systems' motion in sync.
export { SectionReveal, SectionRevealX } from "@/modules/landig-page/components/motion/SectionReveal";
